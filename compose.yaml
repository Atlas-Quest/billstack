# Use postgres/example user/password credentials

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_DB_PASSWORD}
      POSTGRES_DB: billstack
    ports:
      - "127.0.0.1:5432:5432"
